# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Demo/automation pipeline — keep the scripts and input assets, not what they generate.
# Playwright recordings, screenshots and exported demo reports:
demo/output/
# Remotion launch-video project: its own deps, rendered output, and the footage/report images it
# imports (copied in from demo/output/ by the demo scripts, not source):
demo/video/node_modules/
demo/video/out/
demo/video/public/
